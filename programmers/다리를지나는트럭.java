import java.util.*;

class 다리를지나는트럭 {
    public int solution(int bridge_length, int weight, int[] truck_weights) {
        int answer = 0;

        Queue<Integer> rst = new LinkedList<Integer>();
        Queue<Integer> bridge = new LinkedList<Integer>();

        for (int truck : truck_weights) {
            rst.add(truck);
        }

        while(!rst.isEmpty()) {
        }
        return answer;
    }
}
